CREATE index ID_CLIENT
on CLIENT(ID_CLIENT);

CREATE index ID_LOGEMENT
on LOGEMENT(ID_LOGEMENT);

CREATE index ID_INTERET
on INTERET(ID_INTERET);

CREATE index ID_PROPRIETAIRE
on PROPRIETAIRE(ID_PROPRIETAIRE);

CREATE index ID_PUBLICATION
on PUBLICATION(ID_PUBLICATION);

CREATE index ID_QUARTIER
on QUARTIER(ID_QUARTIER);

CREATE index ID_RENDEZ_VOUS
on RENDEZ_VOUS(ID_RENDEZ_VOUS);

CREATE index ID_VILLE
on VILLE(ID_VILLE);